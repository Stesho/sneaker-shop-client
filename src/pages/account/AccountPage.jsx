import React from 'react';
import styles from './AccountPage.module.scss';

const AccountPage = () => {
  return (
    <main className={[styles.accountWrapper, styles.main].join(' ')}>
      
    </main>
  );
};

export default AccountPage;